{
  "name": "cesupa-thesis-1999",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "npx vite",
    "build": "npx vite build && node scripts/build.js",
    "preview": "npx vite preview"
  },
  "devDependencies": {
    "vite": "^7.2.4"
  }
}
